pipeline {
    agent any

    stages {
        stage('Load YAML Pipeline') {
            steps {
                script {
                    def pipelineScript = readFile('pipeline.yaml')
                    def pipeline = readYaml(text: pipelineScript)
                    load pipeline
                }
            }
        }
    }
}
